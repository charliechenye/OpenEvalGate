schema_version: "1"

run:
  id: run_001
  status: complete

candidate:
  id: presales_candidate
  version: presales-example-v1

evaluation:
  kind: human
  evaluator:
    id: human_review
    role: canonical_example_review

outputs:
  results:
    path: eval_results.csv
